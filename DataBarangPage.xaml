<Page
    x:Class="EclatantLaundry.DataBarangPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:EclatantLaundry"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="DataBarangPage">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Page.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                <GradientStop Offset="0.0" Color="White" />
                <GradientStop Offset="0.3" Color="White" />

                <GradientStop Offset="0.7" Color="#FF7D4D" />

                <GradientStop Offset="1.0" Color="#FF5722" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="40,20,40,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock
                Margin="0,0,0,20"
                FontFamily="Segoe UI Variable"
                FontSize="22"
                FontWeight="SemiBold"
                Text="Data Barang" />

            <Border
                Grid.Row="1"
                Height="40"
                Margin="0,0,0,20"
                Background="White"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                CornerRadius="15">
                <Grid>
                    <TextBlock
                        x:Name="Placeholder"
                        Margin="20,0"
                        VerticalAlignment="Center"
                        Foreground="#888888"
                        Text="🔍 Cari Nama Barang atau Tanggal Kedaluwarsa..."
                        Visibility="{Binding Text.IsEmpty, ElementName=txtSearch, Converter={StaticResource BooleanToVisibilityConverter}}" />

                    <TextBox
                        x:Name="txtSearch"
                        Margin="15,0"
                        VerticalContentAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        FontSize="14"
                        TextChanged="txtSearch_TextChanged" />
                </Grid>
            </Border>

            <Grid Grid.Row="2" Margin="0,10,0,0">

                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <DataGrid
                    x:Name="dgBarang"
                    Grid.Row="0"
                    HorizontalAlignment="Stretch"
                    AlternatingRowBackground="#FAFAFA"
                    AutoGenerateColumns="False"
                    Background="Transparent"
                    BorderThickness="0"
                    CanUserAddRows="False"
                    ColumnWidth="*"
                    GridLinesVisibility="None"
                    HeadersVisibility="Column"
                    HorizontalScrollBarVisibility="Disabled"
                    RowBackground="White"
                    RowHeight="50">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#E0E0E0" />
                            <Setter Property="Height" Value="40" />
                            <Setter Property="Foreground" Value="#333333" />
                            <Setter Property="FontWeight" Value="Bold" />
                            <Setter Property="Padding" Value="10,5" />
                            <Setter Property="BorderThickness" Value="0,0,1,1" />
                            <Setter Property="BorderBrush" Value="#CCCCCC" />
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                        </Style>

                        <Style x:Key="CenterTextStyle" TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                        </Style>

                        <Style TargetType="DataGridRow">
                            <Setter Property="Margin" Value="0,2" />
                            <Setter Property="BorderBrush" Value="#DDDDDD" />
                            <Setter Property="BorderThickness" Value="1" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridRow">
                                        <Border
                                            Margin="0,2"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="10">
                                            <DataGridCellsPresenter ItemsPanel="{TemplateBinding ItemsPanel}" SnapsToDevicePixels="True" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn
                            Width="3*"
                            Binding="{Binding Barang.NAMA_BARANG}"
                            ElementStyle="{StaticResource CenterTextStyle}"
                            FontWeight="SemiBold"
                            Header="Nama Barang" />
                        <DataGridTextColumn
                            Width="2*"
                            Binding="{Binding TGL_KEDALUWARSA, StringFormat=\{0:dd/MM/yyyy\}}"
                            ElementStyle="{StaticResource CenterTextStyle}"
                            FontWeight="SemiBold"
                            Header="Kedaluwarsa" />
                        <DataGridTextColumn
                            Width="1.5*"
                            Binding="{Binding Barang.SATUAN}"
                            ElementStyle="{StaticResource CenterTextStyle}"
                            FontWeight="SemiBold"
                            Header="Satuan" />
                        <DataGridTextColumn
                            Width="1.5*"
                            Binding="{Binding JUMLAH_TERSEDIA}"
                            ElementStyle="{StaticResource CenterTextStyle}"
                            FontWeight="SemiBold"
                            Header="Stok" />

                        <DataGridTemplateColumn Width="100" Header="Hapus">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button
                                        Width="30"
                                        Height="30"
                                        Background="Red"
                                        BorderThickness="0"
                                        Click="btnHapus_Click"
                                        CommandParameter="{Binding}"
                                        Cursor="Hand">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border
                                                    x:Name="btnBorder"
                                                    Background="#FF4D4D"
                                                    CornerRadius="5">
                                                    <TextBlock
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        FontWeight="Bold"
                                                        Foreground="White"
                                                        Text="🗑" />
                                                </Border>

                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="btnBorder" Property="Background" Value="#CC0000" />
                                                    </Trigger>

                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="btnBorder" Property="Background" Value="#8B0000" />
                                                        <Setter TargetName="btnBorder" Property="RenderTransform">
                                                            <Setter.Value>
                                                                <ScaleTransform ScaleX="0.9" ScaleY="0.9" />
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter TargetName="btnBorder" Property="RenderTransformOrigin" Value="0.5,0.5" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel
                    Grid.Row="1"
                    Margin="0,15,20,15"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">

                    <Button
                        x:Name="btnPrev"
                        Width="30"
                        Height="30"
                        Background="#E0E0E0"
                        BorderThickness="0"
                        Click="btnPrev_Click"
                        Content="◀">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="15" />
                            </Style>
                        </Button.Resources>
                    </Button>

                    <TextBlock
                        x:Name="lblPageNumber"
                        Margin="15,0"
                        VerticalAlignment="Center"
                        FontWeight="Bold"
                        Foreground="White"
                        Text="Halaman 1" />

                    <Button
                        x:Name="btnNext"
                        Width="30"
                        Height="30"
                        Background="#E0E0E0"
                        BorderThickness="0"
                        Click="btnNext_Click"
                        Content="▶">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="15" />
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <TextBlock
                    x:Name="lblEmpty"
                    Margin="0,150,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    FontSize="16"
                    FontWeight="SemiBold"
                    Foreground="Black"
                    Opacity="0.7"
                    Text="Data Belum Tersedia..."
                    Visibility="Collapsed" />
            </Grid>
        </Grid>
    </Grid>
</Page>
